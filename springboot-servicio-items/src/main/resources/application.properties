spring.application.name=servicio-items
server.port=${PORT:0}

eureka.instance.instance-id=${spring.application.name}:${spring.application.instance-id:${random.value}}
eureka.client.service-url.defaultZone=http://springboot-servicio-eureka:8761/eureka

spring.config.import=optional:configserver:http://springboot-servicio-config-server:8888

configuracion.texto=local
